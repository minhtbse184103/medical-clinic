# Base URL of the Spring Boot API.
# The backend allows this dev server's origin through app.cors.allowed-origins
# (override there with CORS_ALLOWED_ORIGINS when this app runs elsewhere).
VITE_API_BASE_URL=http://localhost:8080
